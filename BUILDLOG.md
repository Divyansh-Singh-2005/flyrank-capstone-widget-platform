# BUILDLOG - AI usage log

Honest record of where AI helped, where it was wrong, and what I changed.

| Date | Area | How AI helped | What was wrong / what I changed |
|------|------|---------------|---------------------------------|
| 2026-09-15 | Repo setup | Suggested a PowerShell 5.1 BOM-free file helper, gitignore, env layout | Local Postgres already on 5432, so Docker Postgres mapped to 5433. First commit message mentions `.env.example` but the file was missed; added in the next commit instead of rewriting history. |
| 2026-09-15 | Design | Drafted data model, API contracts, pipeline order | Added a dev IP override after realising localhost/Docker IPs cannot be geolocated. |
| 2026-09-15 | Docs | Generated DESIGN.md inside a chat code block | AI nested triple-backtick fences inside the code block, which broke copy/paste and lost formatting in section 8. Rewrote all docs in ASCII with `~~~` fences. |
| 2026-09-15 | Core app | Generated config, models, auth, widget CRUD | Switched DB enums to VARCHAR + CHECK (no native Postgres enum) to keep migrations simple. Cross-tenant access returns 404, not 403, so widget existence is not leaked. || 2026-09-16 | Submission path | Generated the pipeline, rate limiter, geo chain, worker and PowerShell attack script | AI flagged that deeply nested JSON raises RecursionError (not ValueError) and that NUL bytes / lone surrogates would crash the Postgres JSONB insert; all three now return 422. The per-IP limit made local tests from 127.0.0.1 block each other, so I added a dev-only TRUST_PROXY_HEADERS. The first PowerShell helper hid 4xx bodies (PS 5.1 consumes the error stream); replaced with HttpClient. Env-based geo toggles needed a restart, so I added a dev-only runtime toggle. Geo error logs only include the exception type, because httpx messages contain the visitor IP. |
| 2026-09-16 | Resilience | Diagnosed a test run where the Docker DB became unreachable | Requests hung ~100 s and the worker ~4.5 min per attempt, because there was no connect timeout. Added connect/statement timeouts, a 503 handler for OperationalError, a drain give-up in the worker, and switched DATABASE_URL to 127.0.0.1 (avoids a second ::1 attempt). The AI-written test script had no setup guard: an empty token/widget id cascaded into misleading 422s and a false "same id on replay: True" (null == null). Added fail-fast setup checks. |